/**
 * Declares a typed input port for a node.
 */
export interface NodeInput {
  id: string;
  label: string;
}

/**
 * Declares a typed output port for a node.
 */
export interface NodeOutput {
  id: string;
  label: string;
}
